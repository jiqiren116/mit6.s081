#include "kernel/param.h"
#include "kernel/types.h"
#include "user/user.h"

int main()
{
    printf("[CMY] The primes experiment is difficult. I haven't finished it yet. Leave it blank and do the next experiment.\n");
    exit(0);
}